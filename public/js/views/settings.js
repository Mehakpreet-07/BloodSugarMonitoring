export function renderSettings(root){
  root.innerHTML = `
    <section class="panel">
      <h2>Settings</h2>
      <p class="muted">Configuration page (thresholds, notifications) — coming soon.</p>
    </section>
  `;
}
